name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: python -m pip install -e .[dev]
      - run: python -m harness_engine.cli init
      - run: python -m harness_engine.cli validate
      - run: ruff check .
      - run: pytest
      - run: python -m harness_engine.cli dashboard
      - uses: actions/upload-artifact@v4
        with:
          name: harness-dashboard
          path: |
            docs/dashboard/index.html
            docs/dashboard/status.json
